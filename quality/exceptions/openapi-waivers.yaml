# OpenAPI contract waivers -- each entry must have all required fields.
#
# Fields:
#   api_version          -- (required) affected API version (e.g. v1, v2)
#   owner                -- (required) person responsible
#   justification        -- (required) why this breaking change is waived
#   compatibility_notes  -- (required) migration / backward-compat notes
#   expires              -- (required) YYYY-MM-DD
#
# Example:
#   - api_version: v1
#     owner: backend@cabulous.com
#     justification: Renaming endpoint for consistency; old route redirects 308
#     compatibility_notes: Clients using /api/tasks must update to /api/task-list
#     expires: "2026-10-01"
