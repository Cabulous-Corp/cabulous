default_install_hook_types: [pre-push]

repos:
  - repo: local
    hooks:
      - id: make-format
        name: make format (service)
        entry: make -C service format
        language: system
        files: ^service/
        pass_filenames: false
        stages: [pre-push]
