default_install_hook_types: [pre-push]

repos:
  - repo: local
    hooks:
      - id: make-lint
        name: make lint (service)
        entry: make -C service lint
        language: system
        files: ^service/
        stages: [pre-push]
